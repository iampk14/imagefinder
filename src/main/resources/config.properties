# src/main/resources/config.properties

# Threading
crawler.thread.count=8

# Politeness (ms)
crawler.politeness.delay=200

# Max pages to crawl
crawler.max.pages=100

# HTTP timeouts (ms)
crawler.http.timeout=10000

# User‑Agent header
crawler.user.agent=ImageFinderBot/1.0

crawler.max.depth=3

